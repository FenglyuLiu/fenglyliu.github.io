export function seo({ title = 'Portfolio', description = 'Personal portfolio', image = '/og-default.png', url = '/' }) {
    return (
    {
    title,
    description,
    image,
    url
    }
    )
    }